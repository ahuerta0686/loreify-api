import { Stories } from '../../../both/collections/story.collection';

import '../publications/story';
import '../publications/blurb';

export class Main {
  start(): void {
    this.initFakeData();
  }

  initFakeData(): void {
    
  }
}
